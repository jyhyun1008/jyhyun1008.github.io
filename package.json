{
  "name": "blog",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "npm run generate && npx gh-pages -d .output/public"
  },
  "dependencies": {
    "@nuxt/content": "^3.8.0",
    "better-sqlite3": "^12.4.1",
    "nuxt": "^4.2.0",
    "vue": "^3.5.22",
    "vue-router": "^4.6.3"
  },
  "resolutions": {
    "nitropack": "2.8.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
